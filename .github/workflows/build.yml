name: Build YataiDON (Release)

on:
  workflow_dispatch:

jobs:
  build-linux:
    name: Build on Linux
    runs-on: ubuntu-latest

    steps:
      - name: Configure Git credentials
        run: |
          git config --global url."https://${{ secrets.GITEA_USER }}:${{ secrets.GITEA_TOKEN }}@ese.tjadataba.se/".insteadOf "https://ese.tjadataba.se/"

      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: false

      - name: Get submodule hash
        id: submod
        run: |
          echo "sha_ptg=$(git ls-tree HEAD Skins/PyTaikoGreen | awk '{print $3}')" >> $GITHUB_OUTPUT
          echo "sha_red=$(git ls-tree HEAD Skins/YataiDONRed | awk '{print $3}')" >> $GITHUB_OUTPUT
          echo "sha_nijiiro=$(git ls-tree HEAD Skins/YataiDONNijiiro | awk '{print $3}')" >> $GITHUB_OUTPUT

      - name: Cache submodule
        id: cache-submod
        uses: actions/cache@v4
        with:
          path: |
            Skins/PyTaikoGreen
            Skins/YataiDONRed
            Skins/YataiDONNijiiro
          key: submod-${{ steps.submod.outputs.sha_ptg }}-${{ steps.submod.outputs.sha_red }}-${{ steps.submod.outputs.sha_nijiiro }}

      - name: Fetch submodule
        if: steps.cache-submod.outputs.cache-hit != 'true'
        run: git submodule update --init --recursive

      - name: Install dependencies
        env:
          DEBIAN_FRONTEND: noninteractive
          NEEDRESTART_MODE: a
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            cmake \
            ninja-build \
            pkg-config \
            patchelf \
            libx11-dev \
            libxrandr-dev \
            libxinerama-dev \
            libxcursor-dev \
            libxi-dev \
            libxtst-dev \
            libgl1-mesa-dev \
            libasound2-dev \
            libpulse-dev \
            libjack-dev \
            liblua5.4-dev \
            libavcodec-dev \
            libavformat-dev \
            libavutil-dev \
            libswscale-dev \
            libswresample-dev \
            libavfilter-dev \
            libavdevice-dev \
            libogg-dev \
            libvorbis-dev \
            libflac-dev \
            libopus-dev \
            libmp3lame-dev \
            libmpg123-dev \
            libspeex-dev \
            libdw-dev

      - name: Setup SDL3
        id: sdl
        uses: libsdl-org/setup-sdl@main
        with:
          install-linux-dependencies: true
          version: 3-head
          build-type: Release

      - name: Configure CMake
        run: |
          cmake -B build \
            -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_CXX_COMPILER=g++ \
            -DCMAKE_C_COMPILER=gcc \
            -DNETWORK_URL="${{ secrets.NETWORK_URL }}" \
            -DNETWORK_AUTH_KEY="${{ secrets.NETWORK_AUTH_KEY }}"

      - name: Build
        run: cmake --build build --config Release

      - name: Prepare release package
        run: |
          mkdir -p release/YataiDON-Linux
          cp build/bin/YataiDON release/YataiDON-Linux/
          cp -r Skins release/YataiDON-Linux/
          cp -r shader release/YataiDON-Linux/
          cp -r Songs release/YataiDON-Linux/
          cp config.toml release/YataiDON-Linux/
          cp update.sh release/YataiDON-Linux/
          chmod +x release/YataiDON-Linux/YataiDON release/YataiDON-Linux/update.sh

          # Bundle FFmpeg shared libraries into lib/
          mkdir -p release/YataiDON-Linux/lib
          ldd build/bin/YataiDON | grep -E "(libav|libsw).*=>" | awk '{print $3}' | \
            xargs -I{} cp -L {} release/YataiDON-Linux/lib/

          # Bundle sndfile codec dependencies (ogg, vorbis, FLAC, opus, mp3, etc.)
          ldd build/bin/YataiDON | grep -E "(libsndfile|libogg|libvorbis|libFLAC|libopus|libmp3lame|libmpg123|libspeex).*=>" | awk '{print $3}' | \
            grep -v "^$" | xargs -I{} cp -L {} release/YataiDON-Linux/lib/ 2>/dev/null || true

          # Bundle SDL3 shared library
          ldd build/bin/YataiDON | grep -E "libSDL3.*=>" | awk '{print $3}' | \
            grep -v "^$" | xargs -I{} cp -L {} release/YataiDON-Linux/lib/ 2>/dev/null || true

          # Bundle transitive deps of FFmpeg AND sndfile codec libs
          for so in release/YataiDON-Linux/lib/lib{av,sw,ogg,vorbis,FLAC,opus,mp3lame,mpg123,speex,SDL3}*.so*; do
            ldd "$so" 2>/dev/null | grep "=>" | awk '{print $3}' | grep -v "^$" | \
              grep -vE "lib(c\.so|m\.so|dl\.so|rt\.so|pthread\.so|gcc_s\.so|stdc\+\+\.so|X11\.so|GL\.so|asound\.so|pulse\.so|jack\.so|Xi\.so|Xrandr\.so|xcb)" | \
              grep -v "ld-linux" | \
              xargs -I{} cp -L {} release/YataiDON-Linux/lib/ 2>/dev/null || true
          done

          # Patch rpath: binary looks in $ORIGIN/lib, bundled .so files look in $ORIGIN
          patchelf --set-rpath '$ORIGIN/lib' release/YataiDON-Linux/YataiDON
          for so in release/YataiDON-Linux/lib/*.so*; do
            [ -f "$so" ] && patchelf --set-rpath '$ORIGIN' "$so" 2>/dev/null || true
          done

          # Checksums — binary + updater + shader + lib
          (
            cd release/YataiDON-Linux
            sha256sum YataiDON update.sh
            find shader -type f | sort | xargs sha256sum
            [ -d lib ] && find lib -type f | sort | xargs sha256sum || true
          ) > release/YataiDON-Linux/checksums-linux.sha256

      - name: Upload Linux package
        uses: actions/upload-artifact@v4
        with:
          name: YataiDON-Linux
          path: release/YataiDON-Linux/
          if-no-files-found: error

  build-windows:
    name: Build on Windows
    runs-on: windows-latest

    defaults:
      run:
        shell: msys2 {0}

    steps:
      - name: Configure Git credentials
        shell: bash
        run: |
          git config --global url."https://${{ secrets.GITEA_USER }}:${{ secrets.GITEA_TOKEN }}@ese.tjadataba.se/".insteadOf "https://ese.tjadataba.se/"

      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: false

      - name: Get submodule hash
        id: submod
        shell: bash
        run: |
          echo "sha_ptg=$(git ls-tree HEAD Skins/PyTaikoGreen | awk '{print $3}')" >> $GITHUB_OUTPUT
          echo "sha_red=$(git ls-tree HEAD Skins/YataiDONRed | awk '{print $3}')" >> $GITHUB_OUTPUT
          echo "sha_nijiiro=$(git ls-tree HEAD Skins/YataiDONNijiiro | awk '{print $3}')" >> $GITHUB_OUTPUT

      - name: Cache submodule
        id: cache-submod
        uses: actions/cache@v4
        with:
          path: |
            Skins/PyTaikoGreen
            Skins/YataiDONRed
            Skins/YataiDONNijiiro
          key: submod-${{ steps.submod.outputs.sha_ptg }}-${{ steps.submod.outputs.sha_red }}-${{ steps.submod.outputs.sha_nijiiro }}

      - name: Fetch submodule
        if: steps.cache-submod.outputs.cache-hit != 'true'
        shell: bash
        run: git submodule update --init --recursive

      - name: Setup MSYS2
        uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          update: true
          install: >-
            mingw-w64-x86_64-gcc
            mingw-w64-x86_64-cmake
            mingw-w64-x86_64-ninja
            mingw-w64-x86_64-pkg-config
            mingw-w64-x86_64-libsndfile
            mingw-w64-x86_64-libsamplerate
            mingw-w64-x86_64-lua
            mingw-w64-x86_64-ffmpeg
            mingw-w64-x86_64-sdl3
            mingw-w64-x86_64-libidn2
            make

      - name: Configure CMake
        run: |
          cmake -B build \
            -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_CXX_COMPILER=g++ \
            -DCMAKE_C_COMPILER=gcc \
            -DNETWORK_URL="${{ secrets.NETWORK_URL }}" \
            -DNETWORK_AUTH_KEY="${{ secrets.NETWORK_AUTH_KEY }}"

      - name: Build
        run: cmake --build build --config Release

      - name: Prepare release package
        shell: bash
        run: |
          mkdir -p release/YataiDON-Windows
          cp build/bin/YataiDON.exe release/YataiDON-Windows/
          cp build/bin/*.dll release/YataiDON-Windows/ 2>/dev/null || true
          cp -r Skins release/YataiDON-Windows/
          cp -r shader release/YataiDON-Windows/
          cp -r Songs release/YataiDON-Windows/
          cp config.toml release/YataiDON-Windows/
          cp update.ps1 update.bat release/YataiDON-Windows/

          # Checksums — binary + updater + dlls + shader
          (
            cd release/YataiDON-Windows
            sha256sum YataiDON.exe update.ps1 update.bat
            find shader -type f | sort | xargs sha256sum
            find . -maxdepth 1 -name "*.dll" | sort | xargs sha256sum
          ) > release/YataiDON-Windows/checksums-windows.sha256

      - name: Upload Windows package
        uses: actions/upload-artifact@v4
        with:
          name: YataiDON-Windows
          path: release/YataiDON-Windows/
          if-no-files-found: error

  build-macos:
    name: Build on macOS
    runs-on: macos-latest

    steps:
      - name: Configure Git credentials
        run: |
          git config --global credential.helper ""
          git config --global url."https://${{ secrets.GITEA_USER }}:${{ secrets.GITEA_TOKEN }}@ese.tjadataba.se/".insteadOf "https://ese.tjadataba.se/"

      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: false

      - name: Get submodule hash
        id: submod
        run: |
          echo "sha_ptg=$(git ls-tree HEAD Skins/PyTaikoGreen | awk '{print $3}')" >> $GITHUB_OUTPUT
          echo "sha_red=$(git ls-tree HEAD Skins/YataiDONRed | awk '{print $3}')" >> $GITHUB_OUTPUT
          echo "sha_nijiiro=$(git ls-tree HEAD Skins/YataiDONNijiiro | awk '{print $3}')" >> $GITHUB_OUTPUT

      - name: Cache submodule
        id: cache-submod
        uses: actions/cache@v4
        with:
          path: |
            Skins/PyTaikoGreen
            Skins/YataiDONRed
            Skins/YataiDONNijiiro
          key: submod-${{ steps.submod.outputs.sha_ptg }}-${{ steps.submod.outputs.sha_red }}-${{ steps.submod.outputs.sha_nijiiro }}

      - name: Fetch submodule
        if: steps.cache-submod.outputs.cache-hit != 'true'
        run: git submodule update --init --recursive

      - name: Install dependencies
        run: |
          brew install \
            cmake \
            ninja \
            pkg-config \
            ffmpeg \
            sdl3 \
            dylibbundler \
            libogg \
            libvorbis \
            flac \
            opus \
            lame \
            mpg123 \
            speex \
            libsndfile \
            libidn2

      - name: Configure CMake
        run: |
          export PKG_CONFIG_PATH="$(brew --prefix)/lib/pkgconfig${PKG_CONFIG_PATH:+:$PKG_CONFIG_PATH}"
          cmake -B build \
            -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_CXX_COMPILER=clang++ \
            -DCMAKE_C_COMPILER=clang \
            -DCMAKE_PREFIX_PATH="$(brew --prefix)" \
            -DCMAKE_EXE_LINKER_FLAGS="-L$(brew --prefix)/lib" \
            -DNETWORK_URL="${{ secrets.NETWORK_URL }}" \
            -DNETWORK_AUTH_KEY="${{ secrets.NETWORK_AUTH_KEY }}"

      - name: Build
        run: cmake --build build --config Release

      - name: Prepare release package
        run: |
          mkdir -p release/YataiDON-macOS
          cp build/bin/YataiDON release/YataiDON-macOS/
          cp -r Skins release/YataiDON-macOS/
          cp -r shader release/YataiDON-macOS/
          cp -r Songs release/YataiDON-macOS/
          cp config.toml release/YataiDON-macOS/
          cp update.sh release/YataiDON-macOS/
          chmod +x release/YataiDON-macOS/YataiDON release/YataiDON-macOS/update.sh

          # Bundle non-system dylib dependencies
          mkdir -p release/YataiDON-macOS/lib
          dylibbundler -od -b \
            -x release/YataiDON-macOS/YataiDON \
            -d release/YataiDON-macOS/lib \
            -p @executable_path/lib

          # Checksums — binary + updater + shader + lib
          (
            cd release/YataiDON-macOS
            shasum -a 256 YataiDON update.sh
            find shader -type f | sort | xargs shasum -a 256
            [ -d lib ] && find lib -type f | sort | xargs shasum -a 256 || true
          ) > release/YataiDON-macOS/checksums-macos.sha256

      - name: Upload macOS package
        uses: actions/upload-artifact@v4
        with:
          name: YataiDON-macOS
          path: release/YataiDON-macOS/
          if-no-files-found: error

  build-android:
    name: Build Android APK
    runs-on: ubuntu-latest

    steps:
      - name: Configure Git credentials
        run: |
          git config --global url."https://${{ secrets.GITEA_USER }}:${{ secrets.GITEA_TOKEN }}@ese.tjadataba.se/".insteadOf "https://ese.tjadataba.se/"

      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: false

      - name: Get submodule hash
        id: submod
        run: |
          echo "sha_ptg=$(git ls-tree HEAD Skins/PyTaikoGreen | awk '{print $3}')" >> $GITHUB_OUTPUT
          echo "sha_red=$(git ls-tree HEAD Skins/YataiDONRed | awk '{print $3}')" >> $GITHUB_OUTPUT
          echo "sha_nijiiro=$(git ls-tree HEAD Skins/YataiDONNijiiro | awk '{print $3}')" >> $GITHUB_OUTPUT

      - name: Cache submodule
        id: cache-submod
        uses: actions/cache@v4
        with:
          path: |
            Skins/PyTaikoGreen
            Skins/YataiDONRed
            Skins/YataiDONNijiiro
          key: submod-${{ steps.submod.outputs.sha_ptg }}-${{ steps.submod.outputs.sha_red }}-${{ steps.submod.outputs.sha_nijiiro }}

      - name: Fetch submodule
        if: steps.cache-submod.outputs.cache-hit != 'true'
        run: git submodule update --init --recursive

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: "17"
          distribution: "temurin"

      - name: Install NDK r27d and CMake 3.22.1
        run: |
          SDKMANAGER="$ANDROID_SDK_ROOT/cmdline-tools/latest/bin/sdkmanager"
          yes | $SDKMANAGER --licenses >/dev/null 2>&1 || true
          $SDKMANAGER "ndk;27.3.13750724" "cmake;3.22.1"

      - name: Cache FFmpeg for Android arm64
        id: cache-ffmpeg
        uses: actions/cache@v4
        with:
          path: .android-ffmpeg
          key: android-ffmpeg-${{ hashFiles('tools/build_ffmpeg_android.sh') }}

      - name: Build FFmpeg for Android
        if: steps.cache-ffmpeg.outputs.cache-hit != 'true'
        run: |
          export ANDROID_NDK_HOME=$ANDROID_SDK_ROOT/ndk/27.3.13750724
          bash tools/build_ffmpeg_android.sh

      - name: Build OpenSSL for Android
        run: |
          export ANDROID_NDK_HOME=$ANDROID_SDK_ROOT/ndk/27.3.13750724
          bash tools/build_openssl_android.sh

      - name: Cache CMake dependencies
        uses: actions/cache@v4
        with:
          path: .cmake-deps
          key: android-cmake-deps-${{ hashFiles('cmake/deps.cmake') }}
          restore-keys: android-cmake-deps-

      - name: Write local.properties
        run: |
          echo "sdk.dir=$ANDROID_SDK_ROOT" > android/local.properties
          echo "cmake.dir=$ANDROID_SDK_ROOT/cmake/3.22.1" >> android/local.properties

      - name: Decode keystore
        run: |
          echo "${{ secrets.ANDROID_KEYSTORE_BASE64 }}" | base64 -d > android/yataidon-release.jks
          printf 'storeFile=yataidon-release.jks\nstorePassword=%s\nkeyAlias=%s\nkeyPassword=%s\n' \
            "${{ secrets.ANDROID_KEYSTORE_PASSWORD }}" \
            "${{ secrets.ANDROID_KEY_ALIAS }}" \
            "${{ secrets.ANDROID_KEY_PASSWORD }}" \
            > android/keystore.properties

      - name: Verify keystore
        env:
          KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
        run: |
          echo "Keystore SHA256:"
          sha256sum android/yataidon-release.jks

          echo "Keystore size:"
          wc -c < android/yataidon-release.jks

          echo "Testing keystore..."
          keytool -list \
            -keystore android/yataidon-release.jks \
            -storepass "$KEYSTORE_PASSWORD"

      - name: Patch config for Android
        run: sed -i 's/^touch_input = false/touch_input = true/' config.toml

      - name: Build Release APK
        working-directory: android
        env:
          NETWORK_URL: ${{ secrets.NETWORK_URL }}
          NETWORK_AUTH_KEY: ${{ secrets.NETWORK_AUTH_KEY }}
        run: ./gradlew assembleRelease --no-daemon

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: YataiDON-Android
          path: android/app/build/outputs/apk/release/app-release.apk
          if-no-files-found: error

  build-ios:
    name: Build unsigned iOS IPA
    runs-on: macos-15
    env:
      CONFIGURATION: Release
      IOS_DEPLOYMENT_TARGET: "16.3"
      IOS_BUNDLE_IDENTIFIER: com.yataidon.app
      # build_ios.sh disables signing when no development team is supplied.
      IOS_DEVELOPMENT_TEAM: ""

    steps:
      - name: Configure Git credentials
        env:
          GITEA_USER: ${{ secrets.GITEA_USER }}
          GITEA_TOKEN: ${{ secrets.GITEA_TOKEN }}
        run: |
          git config --global credential.helper ""
          git config --global url."https://${GITEA_USER}:${GITEA_TOKEN}@ese.tjadataba.se/".insteadOf "https://ese.tjadataba.se/"

      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: false

      - name: Get submodule hash
        id: submod
        run: |
          echo "sha_ptg=$(git ls-tree HEAD Skins/PyTaikoGreen | awk '{print $3}')" >> "$GITHUB_OUTPUT"
          echo "sha_red=$(git ls-tree HEAD Skins/YataiDONRed | awk '{print $3}')" >> "$GITHUB_OUTPUT"
          echo "sha_nijiiro=$(git ls-tree HEAD Skins/YataiDONNijiiro | awk '{print $3}')" >> "$GITHUB_OUTPUT"

      - name: Cache submodule
        id: cache-submod
        uses: actions/cache@v4
        with:
          path: |
            Skins/PyTaikoGreen
            Skins/YataiDONRed
            Skins/YataiDONNijiiro
          key: submod-${{ steps.submod.outputs.sha_ptg }}-${{ steps.submod.outputs.sha_red }}-${{ steps.submod.outputs.sha_nijiiro }}

      - name: Fetch submodule
        if: steps.cache-submod.outputs.cache-hit != 'true'
        run: git submodule update --init --recursive

      - name: Install build tools
        run: |
          if ! command -v cmake >/dev/null; then
            brew install cmake
          fi
          cmake --version
          python3 --version

      - name: Inspect iOS toolchain
        id: toolchain
        run: |
          xcodebuild -version
          xcrun --sdk iphoneos --show-sdk-version
          echo "xcode=$(xcodebuild -version | shasum -a 256 | awk '{print $1}')" >> "$GITHUB_OUTPUT"

      - name: Cache FFmpeg for iOS arm64
        uses: actions/cache@v4
        with:
          path: .ios-ffmpeg/iphoneos-arm64
          key: ios-ffmpeg-${{ runner.os }}-${{ runner.arch }}-iphoneos-arm64-${{ env.IOS_DEPLOYMENT_TARGET }}-${{ steps.toolchain.outputs.xcode }}-${{ hashFiles('tools/build_ffmpeg_ios.sh') }}

      - name: Build unsigned iOS app
        env:
          NETWORK_URL: ${{ secrets.NETWORK_URL }}
          NETWORK_AUTH_KEY: ${{ secrets.NETWORK_AUTH_KEY }}
        run: |
          ./build_ios.sh device \
            -DNETWORK_URL="$NETWORK_URL" \
            -DNETWORK_AUTH_KEY="$NETWORK_AUTH_KEY"

      - name: Package unsigned IPA
        run: |
          APP_PATH="$GITHUB_WORKSPACE/build-ios-device/bin/Release/YataiDON.app"
          test -f "$APP_PATH/Info.plist"
          test -x "$APP_PATH/YataiDON"
          test "$(/usr/libexec/PlistBuddy -c 'Print :CFBundleSupportedPlatforms:0' "$APP_PATH/Info.plist")" = "iPhoneOS"
          xcrun lipo "$APP_PATH/YataiDON" -verify_arch arm64

          IPA_STAGE="$(mktemp -d "$RUNNER_TEMP/yataidon-ipa.XXXXXX")"
          trap 'rm -rf "$IPA_STAGE"' EXIT
          mkdir -p "$IPA_STAGE/Payload" "$GITHUB_WORKSPACE/release"
          ditto --norsrc "$APP_PATH" "$IPA_STAGE/Payload/YataiDON.app"
          ditto -c -k --norsrc --keepParent "$IPA_STAGE/Payload" "$GITHUB_WORKSPACE/release/YataiDON-iOS-unsigned.ipa"
          (
            cd "$GITHUB_WORKSPACE/release"
            shasum -a 256 YataiDON-iOS-unsigned.ipa > checksums-ios.sha256
          )

      - name: Upload unsigned IPA
        uses: actions/upload-artifact@v4
        with:
          name: YataiDON-iOS
          path: |
            release/YataiDON-iOS-unsigned.ipa
            release/checksums-ios.sha256
          compression-level: 0
          if-no-files-found: error

  build-summary:
    name: Build Summary
    runs-on: ubuntu-latest
    needs: [build-linux, build-windows, build-macos, build-android, build-ios]
    if: always()

    steps:
      - name: Check build status
        run: |
          echo "Linux build: ${{ needs.build-linux.result }}"
          echo "Windows build: ${{ needs.build-windows.result }}"
          echo "macOS build: ${{ needs.build-macos.result }}"
          echo "Android build: ${{ needs.build-android.result }}"
          echo "iOS build: ${{ needs.build-ios.result }}"

          if [[ "${{ needs.build-linux.result }}" != "success" ]] || \
             [[ "${{ needs.build-windows.result }}" != "success" ]] || \
             [[ "${{ needs.build-macos.result }}" != "success" ]] || \
             [[ "${{ needs.build-android.result }}" != "success" ]] || \
             [[ "${{ needs.build-ios.result }}" != "success" ]]; then
            echo "One or more builds failed!"
            exit 1
          fi

          echo "All builds completed successfully!"

  release:
    needs: [build-linux, build-windows, build-macos, build-android, build-ios]
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch'
    steps:
      - name: Download Linux artifact
        uses: actions/download-artifact@v4
        with:
          name: YataiDON-Linux
          path: linux/

      - name: Download Windows artifact
        uses: actions/download-artifact@v4
        with:
          name: YataiDON-Windows
          path: windows/

      - name: Download macOS artifact
        uses: actions/download-artifact@v4
        with:
          name: YataiDON-macOS
          path: macos/

      - name: Download Android artifact
        uses: actions/download-artifact@v4
        with:
          name: YataiDON-Android
          path: android-out/

      - name: Download iOS artifact
        uses: actions/download-artifact@v4
        with:
          name: YataiDON-iOS
          path: ios-out/

      - name: Package release assets
        run: |
          # Full zips for manual download
          (cd linux   && zip -r ../YataiDON-Linux.zip .)
          (cd windows && zip -r ../YataiDON-Windows.zip .)
          (cd macos   && zip -r ../YataiDON-macOS.zip .)
          cp android-out/app-release.apk YataiDON-Android.apk
          cp ios-out/YataiDON-iOS-unsigned.ipa YataiDON-iOS-unsigned.ipa

          # Metadata files
          cp linux/checksums-linux.sha256     ./checksums-linux.sha256
          cp windows/checksums-windows.sha256 ./checksums-windows.sha256
          cp macos/checksums-macos.sha256     ./checksums-macos.sha256
          cp ios-out/checksums-ios.sha256     ./checksums-ios.sha256

          # Linux update package: binary + updater + shader + lib
          UPDATE_LINUX="YataiDON update.sh shader/"
          [ -d linux/lib ] && UPDATE_LINUX="$UPDATE_LINUX lib/"
          tar -czf update-linux.tar.gz -C linux $UPDATE_LINUX

          # Windows update package: binary + updater + dlls + shader
          UPDATE_WIN="YataiDON.exe update.ps1 update.bat shader/"
          for dll in windows/*.dll; do
            [ -f "$dll" ] && UPDATE_WIN="$UPDATE_WIN $(basename "$dll")"
          done
          tar -czf update-windows.tar.gz -C windows $UPDATE_WIN

          # macOS update package: binary + updater + shader + lib
          UPDATE_MACOS="YataiDON update.sh shader/"
          [ -d macos/lib ] && UPDATE_MACOS="$UPDATE_MACOS lib/"
          tar -czf update-macos.tar.gz -C macos $UPDATE_MACOS

      - name: Upload Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            YataiDON-Linux.zip
            YataiDON-Windows.zip
            YataiDON-macOS.zip
            YataiDON-Android.apk
            YataiDON-iOS-unsigned.ipa
            update-linux.tar.gz
            update-windows.tar.gz
            update-macos.tar.gz
            checksums-linux.sha256
            checksums-windows.sha256
            checksums-macos.sha256
            checksums-ios.sha256
          name: "YataiDON [Latest Release]"
          tag_name: "latest"
          make_latest: true
          token: ${{ secrets.GITHUB_TOKEN }}
